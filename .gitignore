# ====================================================================
# 🚀 CarePass プロジェクト最適化版 .gitignore
# ====================================================================

# --- [重要] フロントエンド配信設定 ---
# 通常、ビルド成果物(dist)は除外しますが、今回はDjangoから配信するため
# あえて除外リストから外しています。これによりRenderに画面が届きます。
# frontend/dist/  <-- 意図的にコメントアウトしています

# --- Python / Django ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
# 開発用のDBはGitHubに上げず、Render上のDBを使用する
db.sqlite3
db.sqlite3-journal
# 収集された静的ファイル
staticfiles/
media/

# --- Node.js / React (frontend) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
# Viteの一時キャッシュのみ除外
frontend/.vite/

# --- 環境変数・機密情報 ---
# パスワードやAPIキーが漏洩しないように保護
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- エディタ / OS 固有ファイル ---
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
.DS_Store
Thumbs.db

# --- ログ・一時ファイル ---
*.log
pip-log.txt
.cache/
.pytest_cache/

# --- セキュリティ: シェル履歴とブラウザデータ ---
.bash_history
.browser_data_dir/

# --- 配布用zipファイル ---
*.zip
